# 应用生成报告

## 基本信息

- 最近更新时间：`2026-09-03T09:58:09+08:00`
- 飞书需求：脱敏链接 `https://<tenant>.feishu.cn/docx/<redacted>`
- 读取 revision：`107`
- 来源指纹：`sha256:d4e454239bfd451dc4a0147c3ac693209f947adb41ed2c52dfe6cd6b96f83722`
- 应用 ID：`jushuitan.inventory.export_stock`
- 应用版本：`0.2.0`
- 当前需求哈希：`sha256:2ecfe62d15e2cbc3ae180b4e0acf3ca3cd3e1b5aa6bab1e330f41ba4c8687f14`
- Manifest 状态：`ready_for_push`
- 本轮触发：标准 Preview 只检查当前标签页登录标识，导致浏览器启动后立即失败；开发人员要求标准运行自行确保登录并核对目标账号。
- 本轮边界：完成程序修正、真实候选验证、一次 Preview、本地下载内容核验和公共资产回灌；开发人员已确认审核并授权本轮提交推送。未写 NAS、飞书、数据库或正式 Excel，未执行 Live。

## 实际缺陷与修正

- 0.1.0 标准运行不加载本地登录凭据，只在当前标签页查找库存菜单；持久化 Profile 即使有效，只要启动标签页不是工作台也会误判未登录。
- 旧检查只能证明“某个账号已登录”，不能证明会话属于 `--account` 指定账号。
- 0.2.0 的 Prepare 现在打开配置的登录入口，复用有效会话或在同一浏览器内调用验证过的登录指令，再读取页面身份文本并与本地期望身份比对。
- 登录失败、人机验证或身份不匹配时截图并在 S001 前失败；真实身份只在内存和 Git 忽略的本地配置中使用。
- 本机实际页面展示的是租户/店铺身份，语义不同于登录用户名。真实验证首次严格拒绝用户名回退值后，仅在忽略的 `.env` 与 `stores.local.toml` 中补充独立 `identity_env` 映射；可提交文件未保存真实身份。

## 完整业务流程

1. `Prepare`：打开登录入口，复用正确会话或自动登录，并验证当前页面身份属于命令指定账号。
2. `S001`：直接点击左侧库存入口并验证库存模块状态。
3. `S002`：进入商品库存并验证活动页签和业务 iframe。
4. `S003`：重置历史筛选，把真实 checkbox 集合归一化为仅本地目标品牌选中；使用安全锚点收起品牌下拉并验证不可见。
5. `S004`：点击搜索，验证结果表、唯一目标品牌选中状态和下拉层关闭状态。
6. `S005`：导出前再次验证品牌前置条件，只触发一次库存导出；等待下载完成并返回路径、SHA-256 和字节数。
7. `Resume`：重新执行登录与身份验证，重建失败步骤所需页面状态；已验证的既有下载不会重复导出。

输出仅为本地下载文件引用。应用不包含 NAS、飞书多维表格、数据库或正式 Excel 写入步骤。

## 授权与真实运行记录

- 开发人员授权原文限定：应用 `jushuitan.inventory.export_stock` 0.2.0、执行时需求哈希 `sha256:30e0219675d5fdc4f568268e14a73b3e5bb49d17f701d68f9454e40c0f66dfc8`、账号别名 `STORE_001`、一次真实候选验证和一次 Preview；允许登录、身份读取、库存导航、品牌筛选及一次库存下载；禁止所有指定外部业务写入。
- 授权批次：`account-session-verify-20260903`。
- 运行 ID：`account-session-preview-20260903`。
- 首次执行：已有登录会话可用，但登录用户名回退值与页面租户身份不匹配；`jushuitan.auth.ensure_account_session` 失败关闭，S001–S005 均未开始，生成本地身份不匹配证据。
- 同一运行第一次恢复：本地身份映射生效，Prepare、S001、S002 通过；S003 两次动作未完成后停止，未搜索、未导出。
- 同一运行第二次恢复：Prepare 再次通过，按检查点恢复 S003，随后 S004、S005 成功；S003 累计 4 次尝试，S005 仅 1 次，下载目录只有 1 个文件。
- 最终运行状态：`succeeded`；S001–S005 全部成功。
- 每次进程结束均执行浏览器清理；授权批次随后恢复为未签发状态，不能复用。
- 外部业务写入：`false`。

## Preview 与下载证据

- 成功页面证据：`runs/account-session-preview-20260903/evidence/candidate-resume-succeeded-20260903T012728617033Z-33766c0cfa424ae98c093270526ae3fd.png`（Git 忽略）。
- 页面证据显示本地目标品牌已选中、品牌下拉已关闭、搜索结果总数为 1,330，并显示导出成功。
- 下载：`runs/account-session-preview-20260903/downloads/inventory-export.xlsx`（Git 忽略）。
- 下载大小：`218367` 字节。
- 下载 SHA-256：`sha256:bcd6cc8e21af97f2c81d8d2d75e7bdeb0955b5cb17fca3ef86d9448897ec9cc9`。
- 使用只读表格检查导入 XLSX：`Sheet1` 为 1,331 行、47 列，无公式错误；1 行表头加 1,330 条数据，与页面结果数完全一致。
- 导出格式不包含独立品牌列，因此不能以工作簿中的品牌文本计数证明筛选；页面选中状态、页面结果数与导出数据行数三项交叉一致构成本次筛选证据。
- 成功截图 SHA-256：`sha256:b104131981f490f99c798a5e9a6e869345d4d4c847f9fb27e113d15b12bebe44`，大小 `786412` 字节。

## 需求基线与证据边界

- 业务需求和程序版本仍为 0.2.0；真实运行使用开发人员授权的需求哈希 `sha256:30e0219675d5fdc4f568268e14a73b3e5bb49d17f701d68f9454e40c0f66dfc8`。
- 真实验证后，Memory/Spec 仅把两个候选状态更新为已解决，并把来源所有权切换为公共库；由此规范化需求哈希变为 `sha256:2ecfe62d15e2cbc3ae180b4e0acf3ca3cd3e1b5aa6bab1e330f41ba4c8687f14`。
- 哈希变化没有改变浏览器动作、定位器、指令输入输出、成功条件或业务代码；公共指令 Python 文件只将模块说明从“候选”改为“已验证”。因此本次 Preview 是公共回灌前同一实现的有效测试证据，开发人员已明确接受其覆盖当前审核哈希。
- 0.1.0 审核记录 `reviews/20260902T165014+0800.toml` 保留为历史；0.2.0 当前审核记录为 `reviews/20260903T095809+0800.toml`。

## 元素、指令与快照

- 顶层公共库现为 17 个验证元素和 7 条验证指令；24/24 项均进入当前唯一应用的冻结快照，无孤立资产。
- 新公共元素：`jushuitan.erp.shell.account_identity_surface`，版本 `0.1.0`，内容哈希 `sha256:41d74c01ef56ddc46cc2b32f97c215b8e1f878ce3e4c2c29ef155aa104f2bda1`。
- 新公共指令：`jushuitan.auth.ensure_account_session`，版本 `0.1.0`，内容哈希 `sha256:2bb4a43d25787a11aacf034452b9c7e0801cb3facecf76ff10b2783339f0ea5b`。
- 元素真实验证：`xpath://body` 在已登录壳层唯一、可见、可读；错误期望身份被拒绝；正确本地身份在多次恢复中稳定通过。该元素为只读文本载体，不要求点击。
- 指令真实验证：正确会话放行，错误身份失败关闭，Prepare 通过后完整 Preview 成功；真实身份未写日志、检查点、报告或 Git。
- 应用快照共 24 项：17 个元素、7 条指令，全部为 `repository/verified`，内容与顶层来源一致。
- `catalog.lock.json` SHA-256：`sha256:1cb599b6a4e40ae11654fc4888f6ef35dc1e8b82ad6406719ac8ffdb06076eeb`；24/24 项验证通过。
- `UE-ACCOUNT-IDENTITY` 与 `UI-ENSURE-ACCOUNT-SESSION` 均已解决；开放待确认项、元素和指令为 0。
- 人机验证专用元素未在本次真实流程出现，未虚构或沉淀定位器。

## 本地配置

- `StoreConfig.identity_env` 用于页面可见的稳定账号/租户身份文本；未设置时兼容回退至登录账号。
- 本次真实页面证明回退值不适用于当前账号，因此已在 Git 忽略的本地配置中设置独立身份变量。
- `.env.example` 和 `stores.example.toml` 只保留通用占位符；真实账号、密码、身份、店铺和品牌均未进入可提交文件。

## 修改文件范围

- 应用契约与文档：`app.toml`、`README.md`、本报告、Memory、Spec、配置 Schema 与示例。
- 应用程序：版本、模型、Prepare、真实运行编排、元素与指令注册表及冻结快照。
- 应用测试：契约、快照、账号身份、完整流程、恢复和真实运行编排测试。
- 公共库新增：账号身份文本元素，以及“确保目标账号会话”指令目录。
- 应用内旧 `jushuitan.auth.require_session` 快照已删除；引用审计确认所有应用锁和公共依赖均无引用后，顶层旧公共指令也已退役，可由 Git 历史恢复。
- 本轮没有修改 `rpa-core` 源码。

## 自动测试与门禁

- 应用测试：`26 passed`。
- `rpa-core` 回归：`138 passed`。
- 合计：`164 passed`。
- Memory/Spec 字段差异：`0`；计算哈希与 Memory、Spec、Manifest 一致。
- 公共目录：24 项，ID 无冲突、无孤立资产，依赖闭包有效。
- 应用快照：24/24 通过，全部为公共验证资产。
- `rpa-app check`：退出码 `0`，`issues = []`。
- 架构边界、敏感内容、误提交本地路径：无问题。
- `uv lock --check`：通过。
- `git diff --check`：通过。
- Live：未授权、未运行。

## 当前结论

- 需求修正与程序实现：完成。
- 有效会话复用和目标账号身份验证：本轮真实通过；会话失效时调用登录指令的编排由 Fake 测试覆盖，依赖的登录指令沿用 0.1.0 真实验证证据，本轮未主动清除有效会话或重复提交凭据。
- 目标品牌筛选、搜索与一次库存下载：真实 Preview 通过。
- 下载内容与页面记录数交叉核验：通过。
- 公共资产回灌与应用快照重建：完成。
- 外部业务写入：未执行；需求也不包含此类写入。
- 当前状态：`ready_for_push`。
- 开发人员审核：0.2.0 已明确审核通过，并接受运行 `account-session-preview-20260903` 的既有 Preview 证据用于当前需求哈希。
- 提交和推送：开发人员已授权本轮将全部需求相关修改提交并推送至 `origin/main`。
