schema_version = 1

reviewer = "wjiajian"
reviewed_at = "2026-09-03T09:18:30Z"
conclusion = "approved"

app_id = "jushuitan.inventory.export_stock"
app_version = "0.4.0"
requirement_hash = "sha256:02de868803b5daf6eb76d4e66abfad385e20eddb9578a133a4285fc31cd30199"
requirement_revision = 107

preview_run_id = "preview-20260903T090704Z"
element_verification_run_id = "verify-elements-20260903T085838Z"
mode = "preview"
external_business_writes_executed = false
read_back_verification = "not_applicable"

[evidence]
offline_tests = "285 passed (app 68 + rpa-core 217)"
element_verification = "6 stages reached, 19 elements checked, 0 stale, 2 flagged weak"
preview_steps_completed = "S001,S002,S003,S004,S005"
download_sha256 = "sha256:c949b8a959c2e27b701bcbf2cb7fefd99a04ab4e157efa25633456383575a8bc"
download_size_bytes = 218251
secret_scan = "business data / credentials / machine paths / phone numbers: 0 hits"

# 本次审核明确覆盖的结论
[scope_covered]
brand_filter_verified = "S003/S004 从 DOM 回读的选中品牌恰好等于配置品牌；brands_after_search 取自重新归一化之前，证明平台未在搜索后重置选择"
falsifiable_assertions = "5 个 Step 共 16 个反例，每个反例下 execute 抛错或 verify 返回 false"
element_catalog = "19 条 elements.toml 条目在真实页面上按 check_at 阶段逐条比对通过"

# 本次审核明确不覆盖的结论
[scope_not_covered]
export_content_not_cross_verified = "导出 xlsx 无品牌列，无法从文件侧证明筛选；1330 行款式编码前缀一致仅为旁证，不排除该店铺商品本来同前缀"
live_unsupported = "本应用无 external business write，Live 始终关闭，本次未验证任何写入路径"
known_flake = "S003 品牌多选组件偶发 browser_element_action_failed，2026-09-03 两次独立出现，由重试策略覆盖；频率上升需在 _ensure_exact_custom_selection 加等待"
weak_assertions = "account_identity_surface(//body, 实测匹配 9) 与 filter_applied_marker(css:table tbody) 标记为 weak，均不得用于 Step 断言"

# 0.3.0 的审核结论已被推翻，仅保留为历史证据
[superseded]
record = "reviews/20260903T143849+0800.toml"
reason = "该审核签署的运行中 S003 为假执行器（硬编码 selection_visible=True）、S004 为假验证器（css:table tbody 搜索前后同为 21 行），'品牌筛选已验证'不成立"
